/** Increment when persisted project shape changes. */
export const PROJECT_SCHEMA_VERSION = 2;

/** Last schema version that can be migrated forward automatically. */
export const PROJECT_MIN_SUPPORTED_SCHEMA_VERSION = 1;
