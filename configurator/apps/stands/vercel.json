{
  "framework": "vite",
  "installCommand": "npm install --prefix ../..",
  "buildCommand": "npm run build:stands --prefix ../..",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
